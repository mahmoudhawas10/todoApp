<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>To-Do List</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <h1>To Do List</h1>

  
  <form action="/addtask" method="POST">
    <input type="text" name="newtask" placeholder="New Task">
    <button type="submit">Add Task</button>
  </form>

  <h3>Tasks:</h3>
  <ul>
    <% for (let i = 0; i < tasks.length; i++) { %>
      <li>
       
        <input type="checkbox" name="tasks" value="<%= tasks[i] %>">
        
        
        <%= tasks[i] %>

       
        <form action="/edittask/<%= tasks[i] %>" method="GET" style="display:inline;">
          <button class="edit-btn" type="submit">Edit</button>
        </form>

        
        <form action="/deletetask" method="POST" style="display:inline;">
          <input type="hidden" name="task" value="<%= tasks[i] %>">
          <button class="delete-btn" type="submit">Delete</button>
        </form>
      </li>
    <% } %>
  </ul>


  
</body>
</html>
